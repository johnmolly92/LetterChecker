<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".TeacherLoggedIn" >

    <Button
        android:id="@+id/manageStudentsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="56dp"
        android:text="Manage Students" />

    <Button
        android:id="@+id/viewResultsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/chooseLessonsBtn"
        android:layout_alignRight="@+id/chooseLessonsBtn"
        android:layout_below="@+id/chooseLessonsBtn"
        android:layout_marginTop="29dp"
        android:text="View Results" />

    <Button
        android:id="@+id/chooseLessonsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/manageStudentsBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="Choose Lessons" />

    <Button
        android:id="@+id/LogoutBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/viewResultsBtn"
        android:layout_alignRight="@+id/viewResultsBtn"
        android:layout_below="@+id/viewResultsBtn"
        android:layout_marginTop="50dp"
        android:text="Logout" 
        android:background="#FF0000"/>

</RelativeLayout>